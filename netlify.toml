[build]
  publish = "."

[# ベーシック認証は Edge Functions で実装します]
[context.production]
  environment = { NODE_ENV = "production" }

[# 開発環境用の環境変数]
[context.deploy-preview]
  environment = { NODE_ENV = "staging" }

[edge_functions]
  directory = "netlify/edge-functions"
